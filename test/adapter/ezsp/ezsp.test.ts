import {Ezsp} from '../../../src/adapter/ezsp/driver/ezsp';


describe('EZSP Layer', () => {
    let ezsp: Ezsp;

    beforeAll(async () => {
    });

    afterAll(async () => {
    });

    beforeEach(async () => {
        jest.useFakeTimers();
    });

    afterEach(async () => {
        jest.useRealTimers();
    });

    it('TODO', () => {
    });
});

